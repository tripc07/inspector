# OAuth Client URI Configuration
# Controls the client_uri field in OAuth Dynamic Client Registration
# Options:
#   "origin" (default) - Uses window.location.origin (matches redirect_uri origin)
#   ""                 - Omits client_uri entirely (optional per OAuth 2.0 spec)
#   "custom-url"       - Uses the specified URL
# VITE_OAUTH_CLIENT_URI=origin